def init():
    global queue, commands, capture_screen, model_not_compiled, model, score_model, gear_model, position_score_model, time_model, virtual_gamepad, end_epoch, randomplay, randomcommand, randomb
    end_epoch = 0
    # global Queue
    queue = []
    # global command variable
    commands = [0, 0, 0, 0, 0]
    # global capture
    capture_screen = False
